# modblocker

## Header

modblocker

## One-line explainer

DaemonSet manifests that disable the bad `algif_aead` kernel module on immutable Kubernetes worker nodes to mitigate CopyFail.

## Tagline

Disable once, survive every node rebuild.
